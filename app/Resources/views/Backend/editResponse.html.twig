{% extends "Backend/index.html.twig" %}

{% block body %}

    <h4>Modification d'une réponse</h4>

    {{ form_start(form) }}
        <div class="form-group">
            {{ form_row(form.title) }}
        </div>

         <div class="form-group">
            {{ form_row(form.isFair) }}
        </div>

        <div class="form-group">
            {{ form_row(form.question) }}
        </div>

        <button class="btn btn-default" type="submit"> Submit </button>
        <a href="{{ path('response_manage') }}" class="btn btn-default" > Cancel </a>
    {{ form_end(form) }}

{% endblock %}
