{% extends "Backend/index.html.twig" %}

{% block body %}

    <h4>Ajout d'une Video</h4>

    {{ form_start(form) }}
        <div class="form-group">
            {{ form_row(form.title) }}
        </div>
        
        <div class="form-group">
            {{ form_row(form.file) }}
        </div>

        <div class="form-group">
            {{ form_row(form.description) }}
        </div>

        <div class="form-group">
            {{ form_row(form.poolVideo) }}
        </div>

        <button class="btn btn-default" type="submit"> Submit </button>
        <a href="{{ path('video_manage') }}" class="btn btn-default" > Cancel </a>
    {{ form_end(form) }}

{% endblock %}
